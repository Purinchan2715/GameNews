# システム詳細設計書
**プロジェクト名**: ポジティブ・ゲームニュース・アグリゲーションシステム（仮）
**作成日**: 2026年9月3日

## 1. システム概要
ゲームメディアのRSSフィードから最新記事を取得し、AI（Gemini API）を用いてネガティブな内容（炎上、サービス終了、不具合など）をフィルタリング。ポジティブおよびニュートラルな記事のみを抽出したWebサイトを自動生成・公開するシステム。

## 2. システムアーキテクチャ
本システムはサーバーレス・データベースレスの静的サイト生成（Jamstack）アーキテクチャを採用する。

* **実行環境（バッチ処理）**: GitHub Actions
* **公開環境（Webホスティング）**: GitHub Pages
* **開発言語**: Python 3.10+
* **AIモデル**: Gemini API (gemini-1.5-flash または最新の軽量モデル)
* **データ保存**: リポジトリ内のJSONファイル（擬似的なデータベースとして利用）

## 3. データフロー
1. **[トリガー]**: GitHub Actionsがスケジュール（cron）により起動。
2. **[収集]**: Pythonスクリプトが指定された複数のRSSフィードから最新記事を取得。
3. **[差分抽出]**: 取得した記事と、過去の処理履歴（`history.json`）を照合し、**未処理の新規記事のみ**をリストアップ。
4. **[AI判定]**: 新規記事を5〜10件程度のチャンク（塊）に分け、Gemini APIへ送信。ネガティブ判定を行う。
5. **[データ更新]**:
   - 判定結果を履歴（`history.json`）に追記。
   - 合格した記事を公開用データ（`public/news.json`）に追加（※古い記事は一定件数で押し出し削除）。
6. **[静的サイト生成]**: `news.json` を元に、`index.html` を生成（またはフロントエンドのJSが `news.json` を読み込む構成）。
7. **[デプロイ]**: 更新されたファイルをGitHubリポジトリにコミット＆プッシュ。GitHub Pagesが自動でサイトを更新。

## 4. モジュール設計（Pythonスクリプト）

### 4.1. ニュース収集モジュール (`fetcher.py`)
* **機能**: 設定ファイルに記載された複数のRSS URLを巡回し、記事のタイトル、URL、公開日時、概要を取得する。
* **使用ライブラリ**: `feedparser`
* **入力**: RSS URLリスト
* **出力**: 記事オブジェクトのリスト

### 4.2. 履歴管理モジュール (`history_manager.py`)
* **機能**: API消費を抑えるため、すでにAI判定済みの記事をスキップする機能。
* **処理**:
  * ローカルの `history.json`（記事URLのリスト）を読み込む。
  * 収集モジュールで得た記事のうち、`history.json` に存在しない記事URLのみを「新規記事」として抽出。
  * 処理完了後、新規記事のURLを `history.json` に追加して保存する（ファイル肥大化を防ぐため、最新1000件程度を保持）。

### 4.3. AI判定モジュール (`ai_analyzer.py`)
* **機能**: 新規記事リストをGemini APIに送信し、ポジティブ/ネガティブを判定する。
* **API節約ロジック**: 1リクエストで複数記事を判定させる（バッチ処理）。
  * 例: 新規記事が12件ある場合、[5件, 5件, 2件] の3リクエストに分割してAPIを呼び出す。
* **使用ライブラリ**: `google-generativeai`
* **入力**: 新規記事のリスト
* **出力**: ポジティブ判定を受けた記事のリスト

### 4.4. サイト生成・更新モジュール (`site_builder.py`)
* **機能**: 判定を通過した記事を公開用データにマージし、表示用HTMLを生成する。
* **処理**:
  * 既存の `public/news.json` を読み込み、合格した新規記事を先頭に追加。
  * 最大保持件数（例: 100件）を超えた古い記事を削除。
  * （オプション）Jinja2などのテンプレートエンジンを使用し、`index.html` を生成する。

## 5. データ構造・ファイル構成

### リポジトリ構成案
```text
/
├── .github/workflows/
│   └── update.yml        # GitHub Actionsの設定ファイル
├── src/
│   ├── main.py           # メインの実行スクリプト
│   ├── config.json       # RSSのURLリストなどの設定
│   ├── history.json      # 判定済みURLの履歴（DB代わり）
│   └── templates/        # HTMLテンプレートファイル
├── public/               # GitHub Pagesで公開するディレクトリ
│   ├── index.html        # ユーザーがアクセスするページ
│   ├── news.json         # 最新のポジティブニュース一覧
│   └── style.css         # サイトのスタイル
└── requirements.txt      # Python依存ライブラリ
```

### JSONデータ構造例
**`public/news.json` (公開用)**
```json
[
  {
    "title": "待望の新作RPG『〇〇』、来月リリース決定！",
    "url": "https://example.com/news/123",
    "published_at": "2026-09-03T12:00:00Z",
    "source_site": "ゲーム情報局"
  }
]
```

## 6. プロンプト設計 (Prompt Design)
1回のAPI呼び出しで複数記事を判定し、プログラムで扱いやすいJSON形式で返却させる。

**【System Prompt / ユーザープロンプト例】**
```text
あなたはゲームニュースの感情アナリストです。
以下のJSON形式で提供される複数のゲームニュースを分析し、ユーザーを不快にさせるネガティブな要素（サービス終了、発売延期、重篤なバグ、炎上、不祥事、リストラなど）が含まれているか、記事ごとに判定してください。

【入力データ】
[
  {"id": 1, "title": "...", "summary": "..."},
  {"id": 2, "title": "...", "summary": "..."}
]

【出力フォーマット】
必ず以下のJSON配列形式のみを出力してください。Markdownのコードブロック(```json)は不要です。
[
  {"id": 1, "is_negative": false},
  {"id": 2, "is_negative": true}
]
```

## 7. インフラ・実行スケジュール
* **実行環境**: GitHub Actions (`ubuntu-latest`)
* **トリガー**:
  1. `schedule`: cron式 `0 22,10 * * *` (日本時間の毎日7:00と19:00の1日2回)
  2. `workflow_dispatch`: 手動実行用ボタン
* **環境変数管理**: Gemini APIキーは、GitHubリポジトリの `Settings > Secrets and variables > Actions` に `GEMINI_API_KEY` として登録し、コード上には直書きしない。

## 8. エラーハンドリング・制限事項
* **APIレートリミット対策**: 無料枠（例: 1分あたり15リクエスト）を超過しないよう、チャンク処理の間に `time.sleep(5)` 等の意図的な遅延を挿入する。
* **APIエラーのハンドリング**: AI APIが一時的なエラー（500系エラーなど）を返した場合、例外をキャッチしてその回の更新はスキップする（次回実行時にリトライされるため問題ない）。
* **JSONパースエラー**: AIの出力が期待するJSON形式から外れた場合（ハルシネーション等）、正規表現でJSON部分を抽出するか、そのチャンクは安全側に倒して「全NG（スキップ）」として扱う。
